<p>
  <b>Nome:</b>
  <%= @video.nome %>
</p>

<p>
  <b>Descricao:</b>
  <%= @video.descricao %>
</p>

<p>
	<b>Categorias:</b>
	<%= @video.categorias.count %>
</p>
<p>
	<%= @video.authenticated_media_url %>
</p>
<% if @original_video.status == "success" %>
  <video id="movie" width="<%= @h264_encoding.width %>" height="<%= @h264_encoding.height %>" preload="none" 
    poster="<%= @h264_encoding.screenshots.first %>" controls>
    <source src="<%= @video.authenticated_media_url %>" type="video/mp4">
  </video>
<% end %>
<p>
	<b>Imagens:</b>
	<% @video.images.each do |builder| %>		
		<p>
			<%= image_tag builder.image_url %>
		</p>
	<% end %>
</p>
<% if can? :manage, @video %>
	<%= link_to 'Edit', edit_video_path(@video) %> |
<% end %>
<%= link_to 'Back', videos_path %>
