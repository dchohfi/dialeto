<%= simple_form_for(@video, :html => {:multipart => true}) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :nome, :required => true %>
    <%= f.input :descricao %>
    <%= f.association :categorias, :label_method => :descricao %>
		<%= f.association :perfis, :label_method => :descricao %>
		<%= f.label 'Media'%> <%= f.file_field :media %></br></p>
		<%= f.input :tag_list, :label => "Tags"%>

		<%= f.fields_for :images do |builder| %>
			<% if builder.object.new_record? %>
				<p>
					<%= builder.label :image, "Image File" %>
					<%= builder.file_field :image %>
				</p>
			<% end %>
		<% end %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>