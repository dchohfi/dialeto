<h2 class="top-title">Editar Usuário</h2>

<%= simple_form_for @user, :url => user_path, :html => {:id => "insert-form"} do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>