<%= simple_form_for(@categoria) do |f| %>
	<%= f.input :descricao, :label => 'Descrição' %>
	<%= f.association :perfis, :as => :check_boxes, :label_method => :descricao %>
	<%= f.button :submit %>
<% end %>